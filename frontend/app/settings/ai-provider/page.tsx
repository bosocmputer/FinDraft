"use client";

export default function AIProviderSettingsPage() {
  // TODO: ProviderSelector + Model input + API key (masked) + Test Connection
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">ตั้งค่า AI Provider</h1>
      <p className="text-gray-500">TODO: AI Provider settings (admin only)</p>
    </div>
  );
}
