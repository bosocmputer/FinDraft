"use client";

export default function ExportPage({ params }: { params: { id: string } }) {
  // TODO: Export Excel/PDF buttons + export history
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">Export งบการเงิน</h1>
      <p className="text-gray-500">TODO: Export options</p>
    </div>
  );
}
