"use client";

export default function MappingPage({ params }: { params: { id: string } }) {
  // TODO: MappingTable + UnmappedAlert + ConfidenceBadge + Run AI / Confirm buttons
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">ตรวจสอบการ Mapping บัญชี</h1>
      <p className="text-gray-500">TODO: Mapping table</p>
    </div>
  );
}
