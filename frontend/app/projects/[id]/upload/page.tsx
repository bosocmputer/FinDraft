"use client";

export default function UploadPage({ params }: { params: { id: string } }) {
  // TODO: file upload dropzone (Excel/CSV/PDF, max 20MB)
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold mb-4">อัปโหลด Trial Balance</h1>
      <p className="text-gray-500">TODO: File upload</p>
    </div>
  );
}
